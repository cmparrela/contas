{
  "name": "@contas/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "build:dist": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=cjs --platform=node --external:zod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
